#include<stdio.h>
int main()
{
   // char temp;
   // temp = 'G';  // char is always declare with one letter only.
   // printf("%d", temp);  // This will prints the value of 'G' always character constant is represent with single quotes
//
char arr[10];
printf("Enter the string ");
scanf("%s", arr);
gets(arr);
printf("%s", arr);
}

