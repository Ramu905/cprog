#include<stdio.h>
int main()
{
    int a = 500, b = 4000;
    if(a >= b)
    printf(" the problem is correct\n ");
    else
    printf("opps! there are errors in problem\n");
    if(a < b)
    printf("there  are ten mangoes in box\n");
    else 
    printf("there is no mangoes in the box\n");
    return 0;
}
