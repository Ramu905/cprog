#include<stdio.h>
int main()
{
//printing hexadecimal num in decimal format

printf("%d\n", 0x10);
 
//printing hexadecimal num in hexadecimal format

printf("%X\n", 0x10);
 
//printing other format number in hexadecimal format
 
printf("%X\n",16);
return 0;
}
