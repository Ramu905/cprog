#include<stdio.h>
 int main()
 {
int num;
printf("Enter a value");
scanf("%d",&num);
if(num > 0)
    printf("The value is greater than num");
    else
    printf("Enter number is less ");
    return 0;
 
 }