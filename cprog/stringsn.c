#include<stdio.h>
int main()
{
    char name[] = "this is a good book having many lessons which are very useful for life when we read that we will get sucess";
    int i;
   i = sizeof name;
   printf("The length of string is : %d", i);
}