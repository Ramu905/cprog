#include<stdio.h>
int main()
{
    int a, b;
    printf(" Enter first number a %d\n and enter the second number b %d\n", a, b);
    scanf("%d%d",&a, &b);
    if(a < b)
    printf("  Ramu is a good boy\n");
    else 
    printf(" Ramu is a bad boy\n");
    if(a > b)
    printf(" Enter the beatiful world\n");
    else 
    printf(" Stay at the home srceen\n");
    if(a != b)
    printf(" the vlaue is not equal to b");
    else 
    printf("the value is equal to b");
    return 0;


}