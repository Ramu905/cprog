// Declaring a string variable: string variable is a one dimensional character that is capable of holding a 
// string at a time
// Ex; char s[6];  Here always make array one character longer than string make extra room for null char
#include<stdio.h>
int main()
{
    /*char s[6] = {"hello"};   // initializing the string 
    printf("%s", s);
    return 0;
// short length initialization 
char s[7] = {"hello"};
printf("%s", s); */
// long length initialization 
char s[6] = "1F481";
printf("%s", s);  // this is not used in strings
return 0;
}