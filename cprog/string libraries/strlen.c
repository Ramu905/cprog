/* string length function will returns the no.of characters in a string*/
#include<stdio.h>
#include<string.h>
int main()
{
char A[10] = "cprogram";
int l;
l = strlen(A);
printf("the length of string is %d", l);
getchar();
}
