// Accessing elements of an 1D-array //
// Syntax:  array_name[index]    -- index starts from 0 to n-1 length --- 
// To access 1st element of an  array by : arr[0] 2nd: arr[1]
#include<stdio.h>
int main()
{
// -----intializaton of an array-----
// arr[5]= {10,34,56,86,65};
//arr[] = {12,45,56,67,87,...... };
int a[10], i;        
 // If the elements are lesser than length specified then other locations are filled with zeroes //
printf("enter the values of array," a[i]);
scanf("%d",&a[i]);
for(i=0; i<10; i++)
{
    printf("%d\n",a[i]);
}
return 0;
}